# GYMPT 테스트 가이드

> 단위 테스트부터 E2E 테스트까지 전체 테스트 전략 및 실행 가이드

## 목차

1. [테스트 전략](#테스트-전략)
2. [Backend API 테스트](#backend-api-테스트)
3. [Python Services 테스트](#python-services-테스트)
4. [Frontend 테스트](#frontend-테스트)
5. [Lambda 테스트](#lambda-테스트)
6. [통합 테스트](#통합-테스트)
7. [E2E 테스트](#e2e-테스트)
8. [성능 테스트](#성능-테스트)

---

## 테스트 전략

### 테스트 피라미드

```
         /\
        /  \  E2E (5%)
       /____\
      /      \  Integration (15%)
     /________\
    /          \  Unit (80%)
   /____________\
```

### 테스트 목표

- **커버리지:** 80% 이상
- **실행 시간:** CI에서 10분 이내
- **안정성:** Flaky test 0%

### 테스트 환경

| 환경 | 설명 | 사용 |
|------|------|------|
| **로컬** | 개발자 로컬 머신 | 개발 중 즉시 테스트 |
| **CI** | GitHub Actions | PR 시 자동 테스트 |
| **Staging** | AWS Dev 환경 | 통합 테스트 |

---

## Backend API 테스트

### 프로젝트 구조

```
backend-api/src/test/java/com/gympt/backend/
├── unit/                    # 단위 테스트
│   ├── service/
│   │   ├── UserServiceTest.java
│   │   └── WorkoutServiceTest.java
│   └── util/
│       └── JwtTokenProviderTest.java
├── integration/             # 통합 테스트
│   ├── controller/
│   │   ├── AuthControllerTest.java
│   │   └── WorkoutControllerTest.java
│   └── repository/
│       └── UserRepositoryTest.java
└── e2e/                     # E2E 테스트
    └── WorkoutFlowTest.java
```

### Unit Tests (단위 테스트)

**Service Layer 테스트 예시:**

```java
@ExtendWith(MockitoExtension.class)
class UserServiceTest {
    
    @Mock
    private UserRepository userRepository;
    
    @Mock
    private PasswordEncoder passwordEncoder;
    
    @InjectMocks
    private UserService userService;
    
    @Test
    @DisplayName("사용자 생성 - 성공")
    void createUser_success() {
        // given
        CreateUserRequest request = CreateUserRequest.builder()
            .email("test@example.com")
            .password("Password1!")
            .name("홍길동")
            .build();
        
        User savedUser = User.builder()
            .id(1L)
            .email(request.getEmail())
            .name(request.getName())
            .build();
        
        when(userRepository.existsByEmail(request.getEmail()))
            .thenReturn(false);
        when(passwordEncoder.encode(request.getPassword()))
            .thenReturn("encoded_password");
        when(userRepository.save(any(User.class)))
            .thenReturn(savedUser);
        
        // when
        User result = userService.createUser(request);
        
        // then
        assertThat(result.getId()).isEqualTo(1L);
        assertThat(result.getEmail()).isEqualTo("test@example.com");
        
        verify(userRepository).existsByEmail(request.getEmail());
        verify(userRepository).save(any(User.class));
    }
    
    @Test
    @DisplayName("사용자 생성 - 이메일 중복 실패")
    void createUser_duplicateEmail() {
        // given
        CreateUserRequest request = CreateUserRequest.builder()
            .email("existing@example.com")
            .password("Password1!")
            .name("홍길동")
            .build();
        
        when(userRepository.existsByEmail(request.getEmail()))
            .thenReturn(true);
        
        // when & then
        assertThatThrownBy(() -> userService.createUser(request))
            .isInstanceOf(DuplicateEmailException.class)
            .hasMessage("이미 사용 중인 이메일입니다");
        
        verify(userRepository).existsByEmail(request.getEmail());
        verify(userRepository, never()).save(any(User.class));
    }
}
```

### Integration Tests (통합 테스트)

**Controller + Service + Repository 통합 테스트:**

```java
@SpringBootTest
@AutoConfigureMockMvc
@Transactional
class AuthControllerTest {
    
    @Autowired
    private MockMvc mockMvc;
    
    @Autowired
    private ObjectMapper objectMapper;
    
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    @BeforeEach
    void setUp() {
        userRepository.deleteAll();
    }
    
    @Test
    @DisplayName("회원가입 - 성공")
    void signup_success() throws Exception {
        // given
        SignupRequest request = SignupRequest.builder()
            .email("newuser@example.com")
            .password("Password1!")
            .name("신규유저")
            .build();
        
        // when & then
        mockMvc.perform(post("/api/v1/auth/signup")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(request)))
            .andExpect(status().isCreated())
            .andExpect(jsonPath("$.user_id").exists())
            .andExpect(jsonPath("$.email").value("newuser@example.com"))
            .andExpect(jsonPath("$.name").value("신규유저"));
        
        // 데이터베이스 확인
        User savedUser = userRepository.findByEmail("newuser@example.com")
            .orElseThrow();
        assertThat(savedUser.getName()).isEqualTo("신규유저");
    }
    
    @Test
    @DisplayName("로그인 - 성공")
    void login_success() throws Exception {
        // given - 사용자 생성
        User user = User.builder()
            .email("existing@example.com")
            .password(passwordEncoder.encode("Password1!"))
            .name("기존유저")
            .build();
        userRepository.save(user);
        
        LoginRequest request = new LoginRequest(
            "existing@example.com",
            "Password1!"
        );
        
        // when & then
        mockMvc.perform(post("/api/v1/auth/login")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(request)))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.access_token").exists())
            .andExpect(jsonPath("$.refresh_token").exists())
            .andExpect(jsonPath("$.token_type").value("Bearer"))
            .andExpect(jsonPath("$.expires_in").value(900));
    }
}
```

### Repository Tests (JPA 테스트)

```java
@DataJpaTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
class UserRepositoryTest {
    
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private TestEntityManager entityManager;
    
    @Test
    @DisplayName("이메일로 사용자 조회")
    void findByEmail() {
        // given
        User user = User.builder()
            .email("test@example.com")
            .password("encoded_password")
            .name("테스트")
            .build();
        entityManager.persist(user);
        entityManager.flush();
        
        // when
        Optional<User> found = userRepository.findByEmail("test@example.com");
        
        // then
        assertThat(found).isPresent();
        assertThat(found.get().getEmail()).isEqualTo("test@example.com");
    }
}
```

### 테스트 실행

```bash
cd backend-api

# 전체 테스트
./gradlew test

# 특정 테스트 클래스 실행
./gradlew test --tests UserServiceTest

# 특정 테스트 메서드 실행
./gradlew test --tests UserServiceTest.createUser_success

# 커버리지 리포트 생성
./gradlew jacocoTestReport
# 결과: build/reports/jacoco/test/html/index.html

# 커버리지 검증 (80% 미만 시 실패)
./gradlew jacocoTestCoverageVerification
```

---

## Python Services 테스트

### Agent Service 테스트

**프로젝트 구조:**

```
agent-service/tests/
├── unit/
│   ├── test_bedrock_service.py
│   ├── test_recommendation_service.py
│   └── test_utils.py
├── integration/
│   ├── test_api_endpoints.py
│   └── test_database.py
├── fixtures/
│   ├── user_profiles.json
│   └── workout_history.json
└── conftest.py
```

**Unit Test 예시:**

```python
# tests/unit/test_bedrock_service.py

import pytest
from unittest.mock import Mock, patch, AsyncMock
from app.services.bedrock_service import BedrockService
from botocore.exceptions import ClientError

@pytest.fixture
def bedrock_service():
    return BedrockService()

@pytest.mark.asyncio
async def test_generate_workout_recommendation_success(bedrock_service):
    """운동 추천 생성 - 성공"""
    # given
    user_profile = {
        "age": 30,
        "weight_kg": 75,
        "height_cm": 175,
        "fitness_level": "intermediate"
    }
    workout_history = []
    
    mock_response = {
        "body": Mock(read=Mock(return_value=b'''
            {
                "content": [{
                    "text": "추천 운동 계획..."
                }]
            }
        '''))
    }
    
    with patch.object(bedrock_service.client, 'invoke_model', return_value=mock_response):
        # when
        result = await bedrock_service.generate_workout_recommendation(
            user_profile=user_profile,
            workout_history=workout_history
        )
        
        # then
        assert result is not None
        assert "추천" in result

@pytest.mark.asyncio
async def test_generate_workout_recommendation_api_error(bedrock_service):
    """운동 추천 생성 - Bedrock API 오류"""
    # given
    user_profile = {"age": 30}
    
    with patch.object(
        bedrock_service.client,
        'invoke_model',
        side_effect=ClientError(
            {"Error": {"Code": "ThrottlingException"}},
            "invoke_model"
        )
    ):
        # when & then
        with pytest.raises(ClientError):
            await bedrock_service.generate_workout_recommendation(
                user_profile=user_profile,
                workout_history=[]
            )
```

**Integration Test 예시:**

```python
# tests/integration/test_api_endpoints.py

import pytest
from httpx import AsyncClient
from app.main import app

@pytest.mark.asyncio
async def test_recommendation_endpoint():
    """추천 API 엔드포인트 테스트"""
    async with AsyncClient(app=app, base_url="http://test") as client:
        # given
        request_data = {
            "user_id": 1,
            "user_profile": {
                "age": 30,
                "weight_kg": 75,
                "height_cm": 175,
                "fitness_level": "intermediate"
            },
            "workout_history": []
        }
        
        # when
        response = await client.post(
            "/api/v1/recommendations",
            json=request_data
        )
        
        # then
        assert response.status_code == 200
        data = response.json()
        assert "recommendation_id" in data
        assert "workout_plan" in data
        assert data["confidence_score"] > 0

@pytest.mark.asyncio
async def test_recommendation_validation_error():
    """추천 API - 검증 오류"""
    async with AsyncClient(app=app, base_url="http://test") as client:
        # given - 잘못된 데이터
        request_data = {
            "user_id": -1,  # 유효하지 않은 ID
            "user_profile": {}  # 필수 필드 누락
        }
        
        # when
        response = await client.post(
            "/api/v1/recommendations",
            json=request_data
        )
        
        # then
        assert response.status_code == 422
        assert "detail" in response.json()
```

**conftest.py (공통 fixtures):**

```python
# tests/conftest.py

import pytest
import json
from pathlib import Path

@pytest.fixture
def user_profile_fixture():
    """사용자 프로필 테스트 데이터"""
    fixture_path = Path(__file__).parent / "fixtures" / "user_profiles.json"
    with open(fixture_path) as f:
        return json.load(f)

@pytest.fixture
def workout_history_fixture():
    """운동 이력 테스트 데이터"""
    fixture_path = Path(__file__).parent / "fixtures" / "workout_history.json"
    with open(fixture_path) as f:
        return json.load(f)

@pytest.fixture(autouse=True)
def reset_database():
    """각 테스트 전후 데이터베이스 초기화"""
    # Setup: 데이터베이스 초기화
    yield
    # Teardown: 정리
```

### 테스트 실행

```bash
cd agent-service

# 전체 테스트
pytest tests/ -v

# 커버리지와 함께
pytest tests/ -v --cov=app --cov-report=html --cov-report=term

# 특정 테스트만
pytest tests/unit/test_bedrock_service.py -v

# 마커로 필터링
pytest tests/ -v -m "not slow"

# 병렬 실행 (빠른 테스트)
pytest tests/ -v -n auto
```

---

## Frontend 테스트

### 테스트 유형

1. **Unit Tests:** Jest
2. **Component Tests:** React Testing Library
3. **E2E Tests:** Cypress

**프로젝트 구조:**

```
frontend/
├── src/
│   ├── components/
│   │   ├── WorkoutCard.tsx
│   │   └── __tests__/
│   │       └── WorkoutCard.test.tsx
│   └── lib/
│       ├── api-client.ts
│       └── __tests__/
│           └── api-client.test.ts
├── cypress/
│   ├── e2e/
│   │   ├── login.cy.ts
│   │   └── workout-flow.cy.ts
│   └── fixtures/
│       └── users.json
└── jest.config.js
```

### Unit Tests (Jest)

```typescript
// src/lib/__tests__/api-client.test.ts

import { ApiClient } from '../api-client';
import axios from 'axios';

jest.mock('axios');
const mockedAxios = axios as jest.Mocked<typeof axios>;

describe('ApiClient', () => {
  let apiClient: ApiClient;
  
  beforeEach(() => {
    apiClient = new ApiClient('http://test-api.com');
  });
  
  it('should login successfully', async () => {
    // given
    const loginData = {
      email: 'test@example.com',
      password: 'Password1!'
    };
    
    const mockResponse = {
      data: {
        access_token: 'mock_token',
        refresh_token: 'mock_refresh'
      }
    };
    
    mockedAxios.post.mockResolvedValue(mockResponse);
    
    // when
    const result = await apiClient.login(loginData);
    
    // then
    expect(result.access_token).toBe('mock_token');
    expect(mockedAxios.post).toHaveBeenCalledWith(
      '/api/v1/auth/login',
      loginData
    );
  });
});
```

### Component Tests (React Testing Library)

```typescript
// src/components/__tests__/WorkoutCard.test.tsx

import { render, screen, fireEvent } from '@testing-library/react';
import WorkoutCard from '../WorkoutCard';

describe('WorkoutCard', () => {
  const mockWorkout = {
    workout_id: 1,
    title: '상체 운동',
    description: '가슴과 어깨',
    exercises: [
      { name: '벤치프레스', sets: 3, reps: 10 }
    ]
  };
  
  it('should render workout information', () => {
    // when
    render(<WorkoutCard workout={mockWorkout} />);
    
    // then
    expect(screen.getByText('상체 운동')).toBeInTheDocument();
    expect(screen.getByText('가슴과 어깨')).toBeInTheDocument();
    expect(screen.getByText('벤치프레스')).toBeInTheDocument();
  });
  
  it('should call onStart when button clicked', () => {
    // given
    const handleStart = jest.fn();
    render(<WorkoutCard workout={mockWorkout} onStart={handleStart} />);
    
    // when
    const startButton = screen.getByRole('button', { name: /시작/i });
    fireEvent.click(startButton);
    
    // then
    expect(handleStart).toHaveBeenCalledWith(1);
  });
});
```

### E2E Tests (Cypress)

```typescript
// cypress/e2e/login.cy.ts

describe('Login Flow', () => {
  beforeEach(() => {
    cy.visit('/login');
  });
  
  it('should login successfully with valid credentials', () => {
    // given
    cy.intercept('POST', '/api/v1/auth/login', {
      statusCode: 200,
      body: {
        access_token: 'mock_token',
        refresh_token: 'mock_refresh'
      }
    }).as('loginRequest');
    
    // when
    cy.get('input[name="email"]').type('test@example.com');
    cy.get('input[name="password"]').type('Password1!');
    cy.get('button[type="submit"]').click();
    
    // then
    cy.wait('@loginRequest');
    cy.url().should('include', '/dashboard');
    cy.get('[data-testid="user-name"]').should('contain', '홍길동');
  });
  
  it('should show error with invalid credentials', () => {
    // given
    cy.intercept('POST', '/api/v1/auth/login', {
      statusCode: 401,
      body: {
        error: {
          code: 'AUTHENTICATION_ERROR',
          message: '이메일 또는 비밀번호가 올바르지 않습니다'
        }
      }
    });
    
    // when
    cy.get('input[name="email"]').type('wrong@example.com');
    cy.get('input[name="password"]').type('wrongpassword');
    cy.get('button[type="submit"]').click();
    
    // then
    cy.get('[data-testid="error-message"]')
      .should('be.visible')
      .and('contain', '이메일 또는 비밀번호가 올바르지 않습니다');
  });
});
```

### 테스트 실행

```bash
cd frontend

# Unit tests (Jest)
npm run test

# Watch mode (개발 중)
npm run test:watch

# Coverage
npm run test:coverage

# E2E tests (Cypress)
npm run cypress:open  # GUI 모드
npm run cypress:run   # Headless 모드
```

---

## Lambda 테스트

### 테스트 구조

```
lambdas/report-generator/
├── handler.py
├── tests/
│   ├── test_handler.py
│   ├── test_pdf_generator.py
│   └── fixtures/
│       └── sample_session_data.json
└── requirements-dev.txt
```

### Lambda Handler 테스트

```python
# tests/test_handler.py

import json
import pytest
from unittest.mock import patch, Mock
from handler import lambda_handler

@pytest.fixture
def sqs_event():
    """SQS 이벤트 fixture"""
    return {
        "Records": [
            {
                "messageId": "msg-123",
                "body": json.dumps({
                    "session_id": "sess_abc123",
                    "user_id": 1,
                    "workout_id": 456
                })
            }
        ]
    }

def test_lambda_handler_success(sqs_event):
    """Lambda 핸들러 - 성공"""
    # given
    with patch('handler.generate_report') as mock_generate:
        mock_generate.return_value = "s3://bucket/report.pdf"
        
        # when
        response = lambda_handler(sqs_event, {})
        
        # then
        assert response["statusCode"] == 200
        assert "successfully" in response["body"]
        mock_generate.assert_called_once()

def test_lambda_handler_invalid_event():
    """Lambda 핸들러 - 잘못된 이벤트"""
    # given
    invalid_event = {"Records": []}
    
    # when
    response = lambda_handler(invalid_event, {})
    
    # then
    assert response["statusCode"] == 400
```

### 로컬 Lambda 테스트 (SAM CLI)

```bash
cd lambdas/report-generator

# 로컬 실행
sam local invoke ReportGeneratorFunction \
  --event events/sqs-event.json

# API Gateway 로컬 서버
sam local start-api

# 단위 테스트
pytest tests/ -v
```

---

## 통합 테스트

### Backend + Agent Service 통합 테스트

```java
@SpringBootTest
@TestPropertySource(properties = {
    "agent.service.url=http://localhost:${wiremock.server.port}"
})
class WorkoutRecommendationIntegrationTest {
    
    @Autowired
    private WorkoutService workoutService;
    
    @RegisterExtension
    static WireMockExtension wireMock = WireMockExtension.newInstance()
        .options(wireMockConfig().dynamicPort())
        .build();
    
    @Test
    void getAIRecommendation_success() {
        // given - Agent Service mock
        wireMock.stubFor(post(urlEqualTo("/api/v1/recommendations"))
            .willReturn(aResponse()
                .withStatus(200)
                .withHeader("Content-Type", "application/json")
                .withBody("""
                    {
                        "recommendation_id": "rec_123",
                        "workout_plan": { ... }
                    }
                    """)));
        
        // when
        WorkoutPlan plan = workoutService.getAIRecommendation(1L);
        
        // then
        assertThat(plan).isNotNull();
        wireMock.verify(postRequestedFor(urlEqualTo("/api/v1/recommendations")));
    }
}
```

---

## E2E 테스트

### 전체 시스템 E2E 테스트

```typescript
// e2e/workout-session-flow.cy.ts

describe('Complete Workout Session Flow', () => {
  it('should complete entire workout session', () => {
    // 1. 로그인
    cy.login('test@example.com', 'Password1!');
    
    // 2. 운동 계획 선택
    cy.visit('/workouts');
    cy.get('[data-testid="workout-card"]').first().click();
    cy.get('button').contains('시작').click();
    
    // 3. 세션 시작
    cy.url().should('include', '/session/');
    cy.get('[data-testid="camera-view"]').should('be.visible');
    
    // 4. 실시간 피드백 수신 (WebSocket mock)
    cy.window().then((win) => {
      win.mockWebSocketMessage({
        type: 'feedback',
        rep_count: 5,
        score: 85
      });
    });
    
    cy.get('[data-testid="rep-counter"]').should('contain', '5');
    
    // 5. 세션 종료
    cy.get('button').contains('종료').click();
    cy.get('[data-testid="session-summary"]').should('be.visible');
    
    // 6. 리포트 확인
    cy.get('button').contains('리포트 보기').click();
    cy.url().should('include', '/report/');
    cy.get('[data-testid="report-pdf"]').should('exist');
  });
});
```

---

## 성능 테스트

### JMeter / k6 테스트 스크립트

```javascript
// k6-load-test.js

import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '2m', target: 100 }, // Ramp up to 100 users
    { duration: '5m', target: 100 }, // Stay at 100 users
    { duration: '2m', target: 0 },   // Ramp down
  ],
  thresholds: {
    'http_req_duration': ['p(95)<500'], // 95% requests < 500ms
    'http_req_failed': ['rate<0.01'],   // Error rate < 1%
  },
};

export default function () {
  // 로그인
  const loginRes = http.post('https://api.gympt.com/api/v1/auth/login', {
    email: 'test@example.com',
    password: 'Password1!',
  });
  
  check(loginRes, {
    'login status is 200': (r) => r.status === 200,
    'token exists': (r) => r.json('access_token') !== '',
  });
  
  const token = loginRes.json('access_token');
  
  // 운동 목록 조회
  const workoutsRes = http.get('https://api.gympt.com/api/v1/workouts', {
    headers: { Authorization: `Bearer ${token}` },
  });
  
  check(workoutsRes, {
    'workouts status is 200': (r) => r.status === 200,
  });
  
  sleep(1);
}
```

**실행:**

```bash
k6 run k6-load-test.js
```

---

## 테스트 Best Practices

### 1. Arrange-Act-Assert (AAA) 패턴

```java
@Test
void example() {
    // Arrange (given)
    User user = new User("test@example.com");
    
    // Act (when)
    String result = userService.process(user);
    
    // Assert (then)
    assertThat(result).isEqualTo("expected");
}
```

### 2. 테스트 이름 명명 규칙

```
메서드명_시나리오_예상결과

예시:
- createUser_success
- createUser_duplicateEmail_throwsException
- login_invalidPassword_returnsUnauthorized
```

### 3. Test Fixtures 재사용

```python
# conftest.py에 공통 fixture 정의
@pytest.fixture
def sample_user():
    return {
        "email": "test@example.com",
        "name": "테스트유저"
    }

# 여러 테스트에서 재사용
def test_something(sample_user):
    assert sample_user["email"] == "test@example.com"
```

### 4. 테스트 독립성 보장

```java
@BeforeEach
void setUp() {
    // 각 테스트 전 초기화
    repository.deleteAll();
}

@AfterEach
void tearDown() {
    // 각 테스트 후 정리
    repository.deleteAll();
}
```

---

**관련 문서:**
- [개발 가이드](개발가이드.md)
- [배포 가이드](배포가이드.md)

**테스트 문의:** Platform Team  
**최종 업데이트:** 2026-05-19
